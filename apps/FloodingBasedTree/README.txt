To generate topology:

python topo_generator.py "filename" "number of nodes" "graph density(0 - 1)"

The file is created in topologies folder.

To test implementation:

python testscript.py "topology_file"

The figure is produced in figures folder
